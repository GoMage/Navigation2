<div class="block block-gan-categories gan-sidebar-position gan-categories-flyout"
     style="background-color: <?=$block->getDataHelper()->getNavigationViewBlockCategoriesBackgroundColor()?>"
>
    <div class="block-title gan-categories-title" data-role="navigation-filter" data-type="categories-content">
        <strong><?php echo __('Categories') ?></strong>
    </div>
    <div class="block-content block-content-gan-categories">
        <div <? echo ($block->getDataHelper()->isCategoriesShowCollapsed()) ? 'style="display:none;"' : '';?>>
            <ul class="gan-categories-list" <?= ($block->getDataHelper()->isShowCheckboxes()) ? :''; ?>>
                <?php
                foreach($block->getStoreCategories(true, false, true) as $category):
                     if (!$category->getIsActive()) continue;?>
                    <li
                            data-ajax="<?= (int)$block->getDataHelper()->isAjax(); ?>"
                            data-role="navigation-filter"
                            data-type="categories-li"
                            data-url="<?= $block->getCategoryHelper()->getCategoryUrl($category) ?>">
                        <a class="<?= ($block->getCurrentCategoryId() == $category->getId()) ? 'active' : ''; ?>"
                           href="#"> <?= $category->getName() ?></a>
                    </li>
                    <?= $block->getOlList($block->getChildCategories($category)); ?>
                <?php endforeach; ?>
            </ul>
        </div>
    </div>
</div>
