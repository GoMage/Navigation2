<div class="block block-gan-categories gan-categories-dropdown gan-sidebar-position"
     style="background-color: <?=$block->getDataHelper()->getNavigationViewBlockCategoriesBackgroundColor()?>"
>
    <div class="block-title gan-categories-title" data-role="navigation-filter" data-type="categories-content">
        <strong><?php echo __('Categories') ?></strong>
    </div>
    <div class="block-content block-content-gan-categories">
        <div class="gan-categories-list-dropdown" <? echo ($block->getDataHelper()->isCategoriesShowCollapsed()) ? 'style="display:none;"' : '';?>>
            <?php
            foreach($block->getStoreCategories(true, false, true) as $category):
                if (!$category->getIsActive()) continue;?>
                    <select data-role="navigation-filter" data-type="categories-select" data-ajax="<?= (int)$block->getDataHelper()->isAjax(); ?>">
                        <option
                            <?= ($block->getCurrentCategoryId() == $category->getId()) ? 'selected' : ''; ?>
                                value="<?php echo $block->getCategoryHelper()->getCategoryUrl($category) ?>">
                            <?php echo $category->getName() ?>
                        </option>
                        <? if ($block->getDataHelper()->getShowAllSubcategories()): ?>
                            <?= $block->getSelectList($block->getChildCategories($category)); ?>
                        <?endif;?>
                    </select>
            <?php endforeach; ?>
        </div>
    </div>
</div>