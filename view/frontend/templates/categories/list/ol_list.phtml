<?if(!empty($data)):?>
    <ol <?=$block->escapeHtmlAttr($checkboxes)?>>
        <?foreach ($data as $category):?>
            <?php $active = ($currentCategoryId == $category['entity_id']) ? ' class="selected"' : '';?>
                <li
                        data-ajax="<?=$block->escapeHtmlAttr($isAjax)?>"
                        data-role="navigation-filter"
                        data-type="categories-li"
                        data-url="<?=$block->escapeUrl($category['url'])?>"
                        <? if (!empty(($category['children']))): ?> class="parent"<? endif; ?>>
                    <a href="<?=$block->escapeUrl($category['url'])?>"<?=$block->escapeHtml($active)?>><span><?=$block->escapeHtml($category['name'])?></span></a>
                    <? if (!empty(($category['children']))): ?>
                        <?= $categoriesBlock->getOlList($category['children']); ?>
                    <? endif; ?>
                </li>
        <?endforeach;?>
    </ol>
<?endif;?>
