<?
/**
 * @var $categoriesBlock \GoMage\Navigation\Block\Categories
 */

?>

<?foreach ($data as $category):?>
    <? $name = ($categoriesBlock->getDataHelper()->isShowImageName()) ? $category['name'] : ''; ?>
    <ol>
        <li>
            <a href="<?=$categoriesBlock->escapeUrl($category['url'])?>">
                <img
                        src="<?=$categoriesBlock->escapeUrl($categoriesBlock->getCategoryImage($category['entity_id']))?>"
                        width="<?=$categoriesBlock->escapeHtml($width)?>"
                        height="<?=$categoriesBlock->escapeHtml($height)?>"><?=$categoriesBlock->escapeHtml($name)?></a>
            <? if (is_array($category['children']))
                $categoriesBlock->getImageCategoriesList($category['children']);
            ?>
        </li>
    </ol>
<?endforeach;?>
