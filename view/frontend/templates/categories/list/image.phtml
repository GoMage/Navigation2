<?foreach ($data as $category):?>
    <?php $name = ($categoriesBlock->getDataHelper()->isShowImageName()) ? $category['name'] : ''; ?>
    <ol>
        <li>
            <a href="<?=$block->escapeUrl($category['url'])?>">
                <img
                        src="<?=$block->escapeUrl($categoriesBlock->getCategoryImage($category['entity_id']))?>"
                        width="<?=$block->escapeHtmlAttr($width)?>"
                        height="<?=$block->escapeHtmlAttr($height)?>"><?=$block->escapeHtml($name)?></a>
            <?php if (is_array($category['children']))
                $categoriesBlock->getImageCategoriesList($category['children']);
            ?>
        </li>
    </ol>
<?endforeach;?>
