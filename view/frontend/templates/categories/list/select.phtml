<? foreach ($data as $category): ?>
    <?php $selected = ($currentCategoryId == $category['entity_id']) ? 'selected' : ''; ?>
    <option <?= $block->escapeHtmlAttr($selected) ?>
            value="<?= $block->escapeUrl($category['url']) ?>"> <?= $block->escapeHtml($categoriesBlock->addlevelSuffix($category['level']) . $category['name']) ?></option>
    <?php if (is_array($category['children']))
        $categoriesBlock->getSelectList($category['children']);
    ?>
<? endforeach; ?>
